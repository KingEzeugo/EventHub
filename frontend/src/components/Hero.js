import React from 'react';

const Hero = () => {
    return (
        <section id="home" className="hero">
            <div className="container">
                <h2 className="tagline">Discover, Plan, and Join Community Events</h2>
                <p className="subtitle">Your one-stop platform for connecting with your community and exploring events around you.</p>
                <a href="#events" className="cta-button">Explore Events</a>
            </div>
        </section>
    );
};

export default Hero;
